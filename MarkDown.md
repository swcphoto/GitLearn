# MarkDown 语法笔记

# 标题
`#` + 空格 + 标题文字，共支持六级：

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

# 文本样式
- 斜体：`*斜体*` 或 `_斜体_` → *斜体*
- 粗体：`**粗体**` 或 `__粗体__` → **粗体**
- 粗斜体：`***粗斜体***` 或 `___粗斜体___` → ***粗斜体***
- 删除线：`~~删除线~~` → ~~删除线~~
- 行内代码：`` `hello world` `` → `hello world`

# 列表

## 无序列表
`-`、`*`、`+` 三种符号均可，嵌套用缩进（2 或 4 空格）：

```markdown
- 项目一
- 项目二
  - 子项目
```

- 项目一
- 项目二
  - 子项目

## 有序列表
数字 + `.`，嵌套用缩进：

```markdown
1. 第一项
2. 第二项
   1. 子项
```

1. 第一项
2. 第二项
   1. 子项

## 任务列表
`- [ ]` 未完成，`- [x]` 已完成：

```markdown
- [x] 已完成任务
- [ ] 未完成任务
```

- [x] 已完成任务
- [ ] 未完成任务

# 链接和图片
- 插入链接：`[链接文本](https://example.com)` → [链接文本](https://example.com)
- 插入图片：`![图片描述](image.jpg)` → ![图片描述](image.jpg)

# 代码
- 行内代码：`` `hello world` `` → `hello world`
- 代码块：三反引号包裹，可标注语言获得高亮：

````markdown
```c
#include <stdio.h>

int main(void)
{
    printf("hello world!\n");
    return 0;
}
```
````

# 引用
`>` + 空格 + 引用内容，可嵌套（`>>`）：

```markdown
> 引用文本
>> 嵌套引用
```

> 引用文本
>> 嵌套引用

# 分割线
单独一行使用三个及以上的 `-` 或 `*`，行内不能有其他文字：

```markdown
---
***
```

上面是一条分割线：

---

# 表格
`|` 分隔单元格，第二行用 `:` 控制对齐方式：

```markdown
| 左对齐 | 居中对齐 | 右对齐 |
| :----- | :------: | -----: |
| 单元格 |  单元格  | 单元格 |
```

| 左对齐 | 居中对齐 | 右对齐 |
| :----- | :------: | -----: |
| 单元格 |  单元格  | 单元格 |
| 单元格 |  单元格  | 单元格 |

# 转义字符
使用 `\` 转义 Markdown 中的特殊字符：

```markdown
\*不是斜体\*
```

\*不是斜体\*
