# 标签管理

# 创建标签
- 创建标签 `git tag <tag-name>`
- 为指定提交创建标签 `git tag <tag-name> <commit-hash>`
- 为指定提交创建附注标签（带说明） `git tag -a <tag-name> -m "xxx" <commit-hash>`

# 查看标签
- 查看所有标签 `git tag`
- 查看指定标签信息 `git show <tag-name>`

# 推送标签
- 将标签推送到远程 `git push origin <tag-name>`
- 推送所有标签 `git push origin --tags`

# 删除标签
- 删除本地标签 `git tag -d <tag-name>`
- 删除远程标签 `git push origin :refs/tags/<tag-name>`
