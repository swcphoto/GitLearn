# 提交与历史操作

# 提交
- 常规提交 `git commit`
- 修改最近一次提交注释 `git commit --amend -m "msg" `
- 给最近一次提交追加代码，不修改注释 `git commit --amend --no-edit`

# 版本回退
- 回退至上一版本 `git reset --hard HEAD^`
- 回退至某版本 `git reset --hard <commit-hash>`
- 回退后想再回到新版本，可用 `git reflog` 找到对应 hash 再 reset

# 撤销提交
- 撤销最近一次提交，改动保留在暂存区 `git reset --soft HEAD^`
- 撤销最近一次提交，改动回到工作区 `git reset HEAD^`
- 彻底丢弃最近一次提交 `git reset --hard HEAD^`
- 撤销上一次提交（生成一个反向提交，安全，适合已推送的历史） `git revert HEAD`

# rebase（变基）
- 更改当前分支的目标基底 `git rebase <目标基底>`
- 更换指定分支的目标基底 `git rebase <目标基底> <指定分支>`
- 交互式变基（修改最近 4 条提交） `git rebase -i HEAD~4`

# 分离 HEAD
- HEAD 指向某次提交或分支 `git checkout <commit-hash/branch>`
- 相对引用，将 HEAD 指向上一次提交 `git checkout HEAD^`
- 当前节点有两个父节点时，指向不同的父节点 `git checkout HEAD^2`
- 绝对引用，指向往上第 3 次提交 `git checkout HEAD~3`

# 提取单个提交（cherry-pick）
- 将指定提交复制到当前分支 `git cherry-pick <commit-id>...`

# 其他
- 用最近的 tag 描述当前提交（输出如 `v1.0-3-gabcdef`） `git describe`

# 修改历史中的某一条提交
- 交互式变基: git rebase -i HEAD~x/commit-id
- 将待修改的提交: pick -> edit
- 修改代码: git add 
- 修订提交: git commit --amend
- 在vim编辑器中修改提交备注
- 继续完成变基: git rebase --continue
- 放弃变基: git rebase --abort
