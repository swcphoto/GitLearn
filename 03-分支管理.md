# 分支管理

# 查看分支
- 查看本地分支 `git branch`
- 查看所有分支（含上游跟踪信息） `git branch -vv`
- 查看远程分支 `git branch -r`
- 查看所有分支（本地+远程） `git branch -a`

# 创建分支
- 创建新分支 `git branch <branch-name>`
- 创建并切换分支 `git checkout -b <branch-name>`
- 创建并切换分支 `git switch -c <branch-name>`
- 创建分支并直接关联远程分支 `git checkout -b <master> <origin/master>`

# 切换分支
- 切换已有分支 `git checkout <branch-name>`
- 切换已有分支 `git switch <branch-name>`

# 重命名分支
- 重命名当前分支 `git branch -m <new-branch-name>`
- 重命名非当前分支 `git branch -m <old-branch-name> <new-branch-name>`

# 删除分支
- 删除本地分支 `git branch -d <branch-name>`
- 强制删除本地分支（用于未合并分支） `git branch -D <branch-name>`
- 删除远程分支 `git push origin --delete <branch-name>`
- 删除远程分支 `git push origin :<branch-name>`

# 合并分支
- 合并某分支到当前分支 `git merge <branch-name>`
- 合并分支并禁用快进（保留分支拓扑） `git merge --no-ff -m "xxx" <branch-name>`

# 暂存更改（stash）
- 暂存当前工作 `git stash`
- 恢复暂存内容并删除该条 stash `git stash pop`
- 查看暂存列表 `git stash list`
- 恢复指定 stash（不删除） `git stash apply stash@{number}`

# 复制提交
- 将指定提交的修改复制到当前分支 `git cherry-pick <commit-hash>`
- 复制多个提交 `git cherry-pick <commit-id>...`
