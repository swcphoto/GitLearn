# Git 配置

# 配置用户参数
- 配置用户名 `git config --global user.name "newName"`
- 配置邮箱 `git config --global user.email "newEmail"`
- 查看配置参数 `git config --global --list`

# SSH 密钥
- 生成 SSH 密钥 `ssh-keygen -t rsa`
- 测试 SSH 配置是否成功 `ssh -T git@github.com`
