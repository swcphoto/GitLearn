# 文件操作

# 暂存修改（git add）
* 暂存指定文件 `git add <file>`
* 暂存全部修改 `git add .`
* 交互式分块暂存 `git add -p <file>`（不指定文件则对所有已跟踪文件分块选择）

## git add -p（patch 模式）
将修改按 hunk（代码块）拆分，逐块询问是否暂存，适合把一个文件中的多处修改拆成多个提交：

| 选项 | 说明 |
| :---: | :----- |
| y | 暂存当前代码块 |
| n | 跳过当前代码块（不暂存） |
| q | 退出，剩余代码块全部跳过 |
| a | 暂存当前文件剩余的全部代码块 |
| d | 跳过当前文件剩余的全部代码块 |
| s | 将当前代码块拆分成更小的块（块太大且混杂多处修改时使用） |
| e | 手动编辑当前代码块，精确选择要暂存的行 |
| ? | 显示帮助 |

```bash
# 交互式分块暂存
git add -p main.c
# 每个代码块的提示：
# Stage this hunk [y,n,q,a,d,s,e,?]?

# 典型场景：一次改了两个功能，拆成两次提交
git add -p                # 只选功能A相关的块
git commit -m "feat A"
git add -p                # 选剩余功能B的块
git commit -m "feat B"
```

# 删除文件
* 语法 `git rm [option] <file>...`

| 选项 | 全称 | 说明 |
| :-----: | :------: | :-----: |
| -f | --force | 强制删除，即使文件有未暂存的修改 |
| -n | --dry-run | 试运行，显示将要删除的文件但不实际执行 |
| -r | | 递归删除目录及其内容 |
| --cache | | 仅从暂存区删除，保留工作目录中的文件 |
| --ignore-unmatch | | 即使文件不存在也不报错（退出状态为0） |

* 删除文件
    * 删除单个文件 `git rm main.c`
    * 删除多个文件 `git rm main.c test.c`
    * 使用通配符删除 `git rm *.c`
* 删除目录
    * 删除空目录 `git rm dir/`
    * 删除目录及其子内容 `git rm -r dir/`
* 删除暂存区中的文件（保留工作区文件）
    * `git rm --cache <file>`

```bash
git rm file.txt
git commit -m "remove file.txt"
```

# 撤销修改
* 丢弃工作区修改 `git checkout -- <file>`
* 丢弃工作区修改 `git restore <file>`
* 丢弃全部工作区修改 `git restore .`
* 撤销暂存区修改（unstage） `git reset HEAD <file>`
* 丢弃暂存区修改 `git restore --staged <file>`
* 撤销工作区与暂存区修改 `git restore --staged --worktree <file>`
* 撤销工作区与暂存区修改（简写） `git restore -S -W <file>`
* 恢复文件到历史版本 `git restore --source=<commit-hash-or-branch-name> <file>`
